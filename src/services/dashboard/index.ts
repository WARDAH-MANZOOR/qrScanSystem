const merchantDashboardDetails = async (user: any, params: any) => {
  try {
    //      let data = await getDashboardSummary((req.user as JwtPayload)?.id)
    // res.status(200).json(data);
  } catch (error) {}
};

export default {
  merchantDashboardDetails,
};
