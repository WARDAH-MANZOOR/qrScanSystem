import * as $runtime from "@prisma/client/runtime/library"

/**
 * @param timestamp
 * @param timestamp
 */
export const getProfitAndBalance: (timestamp: Date, timestamp: Date) => $runtime.TypedSql<getProfitAndBalance.Parameters, getProfitAndBalance.Result>

export namespace getProfitAndBalance {
  export type Parameters = [timestamp: Date, timestamp: Date]
  export type Result = {
    merchant_id: number
    full_name: string
    company_name: string
    total_balance: $runtime.Decimal | null
    profit: $runtime.Decimal | null
  }
}
