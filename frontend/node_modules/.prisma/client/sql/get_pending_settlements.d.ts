import * as $runtime from "@prisma/client/runtime/library"

/**
 * @param timestamptz
 * @param timestamptz
 */
export const get_pending_settlements: (timestamptz: Date, timestamptz: Date) => $runtime.TypedSql<get_pending_settlements.Parameters, get_pending_settlements.Result>

export namespace get_pending_settlements {
  export type Parameters = [timestamptz: Date, timestamptz: Date]
  export type Result = {
    merchant_id: number
    company_name: string
    full_name: string
    scheduled_task_count: bigint | null
    total_transaction_amount: $runtime.Decimal | null
  }
}
