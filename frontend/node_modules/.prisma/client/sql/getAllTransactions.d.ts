import * as $runtime from "@prisma/client/runtime/library"

/**
 */
export const getAllTransactions: () => $runtime.TypedSql<getAllTransactions.Parameters, getAllTransactions.Result>

export namespace getAllTransactions {
  export type Parameters = []
  export type Result = {
    transaction_id: string
    original_amount: $runtime.Decimal | null
    settled_amount: $runtime.Decimal | null
    profit: $runtime.Decimal | null
    merchant_name: string
  }
}
